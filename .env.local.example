# Anthropic API key (used by /api/chat → AI tutor and the quiz-factory skill)
ANTHROPIC_API_KEY=sk-ant-...

# Site URL (used if you later need absolute URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
